import { AppContextService } from './app-context-service';

describe('AppContextService', () => {
  it('should create an instance', () => {
    expect(new AppContextService()).toBeTruthy();
  });
});
